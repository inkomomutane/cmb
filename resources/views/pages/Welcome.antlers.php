{{ section:seo }}
    <title>{{ title ?? '' }}</title>
    <meta name="content" content="{{ meta_content }}" />
{{ /section:seo }}

{{ section:content }}

    {{ partial:pages/partials/header }}

    {{ partial:pages/partials/hero }}

    {{ /section:content }}
